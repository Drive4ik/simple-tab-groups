<!-- https://github.com/n3r4zzurr0/svg-spinners -->
<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" stroke="#000" viewBox="0 0 24 24" style="color-scheme: light dark;">
    <style>
        @keyframes spinner_one {
            to {
                transform: rotate(360deg);
            }
        }

        @keyframes spinner_two {
            0% {
                stroke-dasharray: 0 150;
                stroke-dashoffset: 0;
            }

            47.5% {
                stroke-dasharray: 42 150;
                stroke-dashoffset: -16;
            }

            95%, to {
                stroke-dasharray: 42 150;
                stroke-dashoffset: -59;
            }
        }

        .wrapper {
            transform-origin: center;
            animation: spinner_one 2s linear infinite;
        }

        circle {
            stroke-linecap: round;
            animation: spinner_two 1.5s ease-in-out infinite;
        }
    </style>

    <g class="wrapper">
        <circle cx="12" cy="12" r="9.5" fill="none" stroke="context-fill light-dark(#5b5b66,#fbfbfe)" stroke-width="3" />
    </g>
</svg>
